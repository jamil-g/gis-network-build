[pytest]
# Lets you run "pytest" from the python/ directory and get full imports (from src.x import y),
# exactly like the CLI (python -m src.cli.main) - see CLAUDE.md, code conventions.
pythonpath = .
testpaths = tests
markers =
    db: requires a real DB (docker compose up -d db) - skipped automatically if unreachable.
