# Reading geographic formats (Shapefile, FGDB, GeoJSON, GeoPackage) and geometry handling
geopandas>=0.14
fiona>=1.9
shapely>=2.0
pyogrio>=0.7        # faster backend for geopandas read/write

# Connection to Postgres/PostGIS/pgRouting
psycopg2-binary>=2.9
sqlalchemy>=2.0
geoalchemy2>=0.14

# CLI
click>=8.1
rich>=13.7           # tidy terminal output (tables, progress bars) - useful for the readiness-score report

# Configuration and validation
pydantic>=2.6
pydantic-settings>=2.2
python-dotenv>=1.0

# Route query: geocoding HTTP calls + the REST API layer
requests>=2.31
fastapi>=0.110
uvicorn[standard]>=0.29

# Tests
pytest>=8.0
httpx>=0.27          # used by FastAPI's TestClient
